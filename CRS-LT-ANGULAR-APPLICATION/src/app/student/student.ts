export  class Student {

    public studentId: string;
    public branch: string;
    public batch: string;
    public address: string;
    public gender: string;
    public name: string;
    public password: string;

    constructor(studentId: string, branch: string, batch: string, address: string, gender: string, name: string, password: string )
         {
  this.studentId=studentId;
  this.batch=batch;
  this.branch=branch;
  this.address=address;
  this.gender=gender;
  this.name=name;
  this.password=password
    }






}